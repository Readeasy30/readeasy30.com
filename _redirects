# Cloudflare Pages redirects for MathEasy30
# Clean URLs handled automatically by Cloudflare Pages — no /app rules (they cause a loop).
# Cloudflare Pages redirects for MathEasy30
# Clean URLs handled automatically by Cloudflare Pages — no /app rules (they cause a loop).
